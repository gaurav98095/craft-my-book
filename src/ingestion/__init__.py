"""
Pipeline A — Document Preprocessing.

Turns raw sources (PDFs, slides, lecture recordings) into a chunked,
tagged-ready corpus: Stage 1 (speech to text) -> Stage 2 (layout parsing) ->
Stage 3 (figure description) -> Stage 4 (consolidation and chunking) ->
Stage 5 (source index).
"""
